data:
  # output dirs for the processed (query, document) pairs
  wiki_dir: data/processed/wiki
  squad_dir: data/processed/squad

  # j&m book chunks for the demo corpus (search index, not training)
  corpus_path: data/processed/corpus.jsonl

  # cleaning / filtering
  min_words: 40
  max_words: 300
  wiki_articles: 15000
  seed: 42
